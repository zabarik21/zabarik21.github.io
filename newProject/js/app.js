 //2 Mutirovanie
 // alert(name + 'Вы очень умный и вам ' + age.toString)
 //const surname = prompt("Введите фамилию")

 /* let birthY = prompt("Введите год рождения") */
 /* let age = nowY - birthY */
 /* alert("Вас зовут " + name + " а фамилия " + surname + "Вам + age + лет") */
 /* const check = true
 console.log(typeof check);
 console.log(typeof age)
 console.log(typeof nowY);
 console.log(typeof name); */
 // Приоритет операторов
 /* const fullage = 26
 const bYear = 2002
 const cYear = 2020
 const isFullAge = (cYear - bYear) >= fullage
 console.log(isFullAge); */
 // Условные Операторы 
 /* const coueseStatus = 'pending' //  ready, fail 
 if (coueseStatus === 'pending') {
   alert("Курс готов")
   console.log("Курс готов")
 } else {
   alert("Курс готовится")
 } */

 /* if (status) {
   alert("")
 } else {
   alert("project is not done ")
 } */
 // Тернарное выражение //
 /* status ? alert("") : alert("project is not done ") */

 /* 
  function calculateAge(Year) {

    return 2020 - Year
  }

  function infoAbout(Year, name) {
    let age = calculateAge(Year)
    if (age > 0) {
      console.log("Ваше имя " + name + " вам " + age + " лет");
    } else {
      console.log("Ты ниггер");
    }

  }
  infoAbout(2222, "Sasha")
  infoAbout(2001, "Straizer")
  infoAbout(20020, "Straizer")
  infoAbout(2001, "Ииуда") */
 // Массивы
 /*       const cars = ["мазок", "ниг", "ищзан", "бензо", "бензо"]
       console.log(cars);
       console.log(cars[2]);
       console.log(cars[2 * 2]);
       cars[0] = 'bigger'
       cars[cars.length] = 'sasha'
       console.log(cars); */

 // 10 Циклы
 /*  const cars = ["мазок", "ниг", "ищзан", "бензо", "бензо"]
  console.log(cars.length);
  for (let i = 0; i <= cars.length; i++) {
    const car = cars[i]
    console.log(car);

  } */
 /*  const cars = ["мазок", "ниг", "ищзан", "бензо", "бензо"]
  for (let car of cars) {
    console.log(car);
    if (car == "мазок") {
      console.log("yes.");
    }
  }
/* /*   */
 /*  const person = {
    firstName: 'Sasha',
    surname: 'ihor',
    hasWife: false,
    greet: function (age) {
      return 2020 - age
    }
  }
  console.log(person.hasWife);
  person.greet(10)
  const ass = 'surname'

  console.log(person.surname);
  person.hasWife = true
  person.nigger = false
  console.log(person);  */
 console.log(2);