/* const name = 'blad'
console.log(typeof name);


function getAge() {
  return age - 1
} */
/* const age = 24
const name = 'bladzenbuagin'
const output = 'Привет меня зовут ' + name + ' и мне ' + age + ' лет.'

console.log(name.toUpperCase());
console.log(name.toLowerCase());
console.log(name.charAt(2));
console.log(name.indexOf("zen"));
console.log(name.startsWith("blad"));
console.log(name.endsWith("blad"));
console.log(name.repeat(3));
const pass = '       password         '
console.log(pass.trimRight());
 */
/* function logPerson(s, name, age) {
  if (age < 0) {
    age = 'минусовой'
  }
  return `${s[0]}${name}${s[1]}${age}${s[2]}`
}
const personName = 'Sasha'
const personAge = -12
const output = logPerson `Имя ${personName}, Возраст: ${personAge}! `
console.log(output);
const speed = 30
const warn = 'Езжайте спокойно'

function Speed(v, Name, Speed, warn) {
  if (speed > 20) {
    warn = 'Осторожней едьте'
  }
  return `${v[0]}${personName}${v[1]}${speed}${v[2]}${warn}`
}
const out = Speed `Ваша ${personName} скорость составляет ${speed} км/ч ${warn}`
console.log(out); */